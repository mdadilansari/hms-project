# Doctors Service

## Description
Provides REST API for doctor data from the hospital management system.

## How to Run
1. Run `node app.js`
2. Visit `http://localhost:3004/doctors`

## Endpoints
- GET /doctors — Lists all doctors
- GET /doctors/:id — Gets details for a specific doctor
